import { redirect } from "next/navigation";

// Content moved to /mapa-talento (scatter chart) and /carpetas (detail table)
export default function EvaluacionesPage() {
  redirect("/mapa-talento");
}
